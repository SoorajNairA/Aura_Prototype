import {expect,test} from "@playwright/test";
test("narration stays contextual and never becomes selection intent",async({page})=>{const part={semanticId:"component-fan",label:"Desk fan",subsystem:"actuator",parentAssembly:"system",representationSource:"procedural model",dimensions:[90,30,90],assembledTransform:{position:[0,0,20]},explodedTransform:{position:[20,0,50]},verificationState:"estimated"};await page.route("**/api/projects/p",r=>r.fulfill({json:{projectId:"p",revision:1,capability:{status:"SUPPORTED"},representations:[]}}));await page.route("**/api/projects/p/assembly",r=>r.fulfill({json:{parts:[part],connectors:[],relationships:[]}}));await page.route("**/api/projects/p/graph",r=>r.fulfill({json:{entities:[{id:"p",kind:"project",name:"Desk Fan"}]}}));await page.route("**/api/projects/p/verification",r=>r.fulfill({json:{summary:{failed:0},findings:[]}}));await page.route("**/api/projects/p/narrations",r=>r.fulfill({json:[{narrationId:"n",text:"The fan is driven through a protected low-voltage stage."}]}));await page.goto("/?projectId=p");await expect(page.getByText(/fan is driven/)).toBeVisible();await expect(page.getByTestId("selected")).toHaveText("");await page.getByRole("button",{name:/Desk fan component-fan/}).click();await expect(page.getByTestId("selected")).toHaveText("component-fan")});
